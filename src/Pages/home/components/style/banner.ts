import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      padding: 12,
      alignItems: 'center',
      height: 240,
    },
    banner: {
      width: '100%',
      height: '100%',
      resizeMode: 'cover',
    },
  });