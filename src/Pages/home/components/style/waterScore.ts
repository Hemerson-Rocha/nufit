import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      padding: 16,
    },
    label: {
      fontSize: 16,
      fontWeight: 'bold',
      color: 'blue',
    },
    progressBar: {
      height: 12,
      backgroundColor: 'darkblue',
      marginTop: 8,
      borderRadius: 10,
    },
  });